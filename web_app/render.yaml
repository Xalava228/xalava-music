services:
  - type: web
    name: xalava-music
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 8080
      - key: FLASK_ENV
        value: production
